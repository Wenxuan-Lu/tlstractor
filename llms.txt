# tlstractor

The R package **tlstractor** implements **TLS-Tractor** (**T**ransfer
**L**earning of **S**ummary Statistics to **Tractor**), a
transfer-learning framework for improving the power of local
ancestry-aware genome-wide association studies (GWAS) in admixed
populations. TLS-Tractor combines internal individual-level
admixed-cohort data with external standard GWAS summary statistics to
improve ancestry-specific effect estimation and overall association
testing.

[Tractor](https://www.nature.com/articles/s41588-020-00766-y) is a local
ancestry-aware GWAS method that estimates ancestry-specific genetic
effects and provides an overall test of genetic association, but its
power is limited by the sample size of available admixed cohorts.
Standard GWAS summary statistics are available at a much larger scale,
but they cannot be directly incorporated into Tractor because the model
parameterization differs. TLS-Tractor bridges this gap by transferring
information from external standard GWAS summary statistics into the
Tractor framework.

Core computational steps are implemented in C++ for efficient analysis
of large datasets. See [Key assumptions](#key-assumptions) before
applying the method.

Last updated: July 3, 2026

Current version: 0.1.0

## Contents

- [Installation](#installation)
- [Quick start](#quick-start)
- [Key assumptions](#key-assumptions)
- [Tutorials and background](#tutorials-and-background)
- [Common usage notes](#common-usage-notes)
- [Installation troubleshooting](#installation-troubleshooting)
- [Citation](#citation)
- [Bug reports and questions](#bug-reports-and-questions)
- [License](#license)

## Installation

`tlstractor` requires **R 4.5.0 or later**. The installation typically
takes a few minutes.

### For Conda users only

If you are not using Conda, skip this subsection and continue to the
installation options below.

To install `tlstractor` in a dedicated Conda environment, run:

``` bash
conda create -n r-tlstractor -c conda-forge r-base -y
conda activate r-tlstractor
conda install -c conda-forge compilers make pkg-config zlib -y
```

### Install `tlstractor`

#### Option 1: Install the development version from GitHub

Use this option to install the current development version.

``` r

install.packages("pak")
pak::pkg_install("Wenxuan-Lu/tlstractor")
```

#### Option 2: Install a specific release

Use this option to install a fixed, reproducible release. Replace
`v0.1.0` with the desired release tag.

``` r

install.packages("pak")
pak::pkg_install("Wenxuan-Lu/tlstractor@v0.1.0")
```

If installation fails, see [Installation
troubleshooting](#installation-troubleshooting).

[Back to Contents](#contents)

## Quick start

This package implements a three-step workflow that is fully compatible
with [Tractor scripts](https://github.com/Atkinson-Lab/Tractor) but
substantially faster. The three steps are: extracts local ancestry
tracts, harmonizes external standard GWAS summary statistics, and runs
TLS-Tractor.

The example below is a minimal runnable template. File names and column
names are illustrative. Please adapt them to your own data. For a
complete pipeline walkthrough, see the [TLS-Tractor
tutorial](https://wenxuan-lu.github.io/tlstractor/), which illustrates
the workflow on simulated datasets and runs in a few seconds.

### Required inputs

Individual-level data from the main study / internal cohort:

- Phased genotypes with local ancestry inference results, provided in
  one of the following formats:

  - For RFMix2/Gnomix local ancestry inference:

    - `cohort.vcf(.gz)`: phased genotypes, with `GT` encoded as phased
      calls such as `0|1`

    - `cohort.msp.tsv(.gz)`: local ancestry labels

  - For FLARE local ancestry inference:

    - `cohort.vcf(.gz)`: FLARE output VCF containing both phased
      genotypes and local ancestry labels

- Phenotype file: `pheno.txt` with columns `id` and `pheno`

- Optional covariate file: `covariates.txt` with columns such as `id`,
  `cov1`, `cov2`, and `cov3`

Summary-level data from the external cohort:

- External standard GWAS summary statistics: `external_sumstats.txt`
  with columns `CHR`, `POS`, `ID`, `REF`, `ALT`, `BETA`, `SE`, and
  optional columns `N`, `AF`, `N_CASE`, `N_CONTROL`, `AF_CASE`, and
  `AF_CONTROL`.

### Minimal example

``` r

library(tlstractor)

# 1) Extract tracts (choose one option)

# Option A: phased VCF + MSP file from RFMix2/Gnomix
extract_tracts(
  vcf_path = "cohort.vcf.gz",
  msp_path = "cohort.msp.tsv",
  num_ancs = 2L,
  output_formats = "gds"
)

# Option B: FLARE VCF
# extract_tracts_flare(
#   vcf_path = "cohort.vcf.gz",
#   num_ancs = 2L,
#   output_formats = "gds"
# )

# 2) Harmonize external standard GWAS summary statistics
munge_sumstats(
  gds_path = "cohort.gds",
  sumstats_path = "external_sumstats.txt",
  match_by = "CHR-POS",
  output_path = "external_sumstats_munged.txt"
)

# 3) Run TLS-Tractor
tlstractor(
  gds_path = "cohort.gds",
  sumstats_path = "external_sumstats_munged.txt",
  method = "linear", # use "logistic" for binary traits
  cond_local = TRUE, # whether to condition on local ancestry terms
  pheno_path = "pheno.txt",
  pheno_id_col = "id",
  pheno_col = "pheno",
  covar_path = "covariates.txt",
  covar_id_col = "id",
  covar_cols = c("cov1", "cov2", "cov3"),
  output_prefix = "tlstractor",
  n_cores = 4L
)
```

### Main outputs

1.  **Tracts GDS** (`cohort.gds`) — HDF5-based GDS file containing:

    - `sample.id` — sample identifiers
    - `snp.chromosome`, `snp.position`, `snp.id`, `snp.ref`, `snp.alt` —
      variant metadata
    - `dosage/anc0..ancK`, `hapcount/anc0..ancK` — ancestry-specific
      alternative-allele dosage (0, 1, 2) and local ancestry haplotype
      count (0, 1, 2) matrices

2.  **Munged summary statistics** (`external_sumstats_munged.txt`) —
    tab-delimited file with columns:

    - `CHR`, `POS`, `ID`, `REF`, `ALT` — variant information
    - `BETA`, `SE` — effect size and standard error
    - Optional columns (when provided): `N`, `AF`, `N_CASE`,
      `N_CONTROL`, `AF_CASE`, `AF_CONTROL` — allele frequencies and
      sample sizes
    - `GDS_ID` — internal variant index linking the summary statistics
      to the GDS file

3.  **TLS-Tractor results** (`tlstractor.txt.gz`) — gzip-compressed
    tab-delimited file with columns:

    **Variant metadata and internal-cohort summaries**

    - `CHROM`, `POS`, `ID`, `REF`, `ALT` — variant information
    - `main_N` — number of internal-cohort samples analyzed
    - `main_N_case`, `main_N_control` — number of internal-cohort cases
      and controls (logistic analysis only)
    - `AF` — overall allele frequency
    - `AF_anc*` — ancestry-specific allele frequencies (e.g., `AF_anc0`,
      `AF_anc1` for two-way admixture)
    - `AF_case_anc*`, `AF_control_anc*` — ancestry-specific allele
      frequencies in cases and controls (logistic analysis only)
    - `LAprop_anc*` — local ancestry proportions at the variant
    - `LAprop_case_anc*`, `LAprop_control_anc*` — local ancestry
      proportions in cases and controls (logistic analysis only)

    **Analysis metadata**

    - `has_sumstats` — logical indicator (`TRUE`/`FALSE`) of whether
      external summary statistics were available for the variant. When
      `FALSE`, the variant was analyzed using internal data alone.
    - `fallback_used` — logical indicator (`TRUE`/`FALSE`) of whether QR
      decomposition fallback was used during computation. When `TRUE`,
      results may have reduced numerical stability and should be
      interpreted cautiously.

    **Association results**

    - `joint_pval` — joint p-value testing all ancestry-specific SNP
      dosage effects
    - `beta_anc*` — ancestry-specific effect estimates (linear
      coefficients for continuous traits and log odds ratios for binary
      traits)
    - `se_anc*` — standard errors of ancestry-specific effects
    - `pval_anc*` — p-values for ancestry-specific effects

    **Local ancestry effects (only present when `cond_local=TRUE`)**

    - `LAeff_anc*` — local ancestry effect estimates
    - `LAse_anc*` — standard errors of local ancestry effects
    - `LApval_anc*` — p-values for local ancestry effect

[Back to Contents](#contents)

## Key assumptions

TLS-Tractor relies on three core assumptions:

1.  **Unrelated individuals in the internal cohort.**

2.  **No sample overlap between internal and external cohorts.**

3.  **Comparable admixture profiles across internal and external
    cohorts.**

    TLS-Tractor relies on a transportability assumption equivalent to
    fixed-effect GWAS meta-analysis: after appropriate covariate
    adjustment, the standard GWAS effect that would be estimated in the
    internal cohort and the standard GWAS effect estimated in the
    external cohort target the same underlying genetic effect, with
    observed differences arising from sampling variation rather than
    true heterogeneity. In practice, this requires comparable admixture
    profiles across cohorts, including the same ancestral components and
    similar global ancestry proportions.

In addition, common GWAS meta-analysis considerations still apply. For
example, the internal and external datasets should have compatible
allele coding, genome build, trait definition, phenotype curation,
sample inclusion criteria, and covariate adjustment.

The package tutorial provides recommended checks for these assumptions
and guidance on interpreting diagnostic results.

[Back to Contents](#contents)

## Tutorials and background

For a complete TLS-Tractor analysis workflow, including input
preparation, tract extraction, summary-statistic harmonization, model
fitting, output interpretation, and assumption checks, see:

- [TLS-Tractor tutorial](https://wenxuan-lu.github.io/tlstractor/)

For background on Tractor, statistical phasing, and local ancestry
inference, see:

- [Tractor tutorial](https://atkinson-lab.github.io/Tractor-tutorial/)

- [Tractor paper](https://www.nature.com/articles/s41588-020-00766-y)

- [Tractor GitHub repository](https://github.com/Atkinson-Lab/Tractor)

Function-level documentation is also available in R:
[`?extract_tracts`](https://wenxuan-lu.github.io/tlstractor/reference/extract_tracts.md),
[`?extract_tracts_flare`](https://wenxuan-lu.github.io/tlstractor/reference/extract_tracts_flare.md),
[`?convert_gds_to_txt`](https://wenxuan-lu.github.io/tlstractor/reference/convert_gds_to_txt.md),
[`?convert_txt_to_gds`](https://wenxuan-lu.github.io/tlstractor/reference/convert_txt_to_gds.md),
[`?munge_sumstats`](https://wenxuan-lu.github.io/tlstractor/reference/munge_sumstats.md),
[`?tlstractor`](https://wenxuan-lu.github.io/tlstractor/reference/tlstractor.md)

[Back to Contents](#contents)

## Common usage notes

### 1. File formats for tract extraction

For tract extraction, `tlstractor` can output three formats: the
original Tractor text format (`.txt` or `.txt.gz`), ancestry-specific
VCF files (`.vcf` or `.vcf.gz`), and a compact GDS format (`.gds`).

GDS is the native input format for TLS-Tractor association analysis. It
stores ancestry-specific allele dosages and local ancestry haplotype
counts in a single file, reducing storage and simplifying downstream
analysis compared with the multiple text files used by standard Tractor
workflows.

The tract-extraction step in `tlstractor` can therefore be used as a
drop-in replacement for existing Tractor workflows. In our tests, the
C++ implementation is at least 35 times faster than the original Tractor
tract-extraction scripts.

If you already have tracts extracted in Tractor-style text format, you
do not need to re-extract them. You can convert them directly to GDS
format using
[`convert_txt_to_gds()`](https://wenxuan-lu.github.io/tlstractor/reference/convert_txt_to_gds.md).
Similarly, GDS files can be converted back to Tractor-style text format
using
[`convert_gds_to_txt()`](https://wenxuan-lu.github.io/tlstractor/reference/convert_gds_to_txt.md).

### 2. Running Tractor with `tlstractor`

In addition to TLS-Tractor analysis, `tlstractor` can fit the Tractor
model using internal individual-level data. The C++ implementation
provides substantially shorter runtime and lower memory usage. See
[`?tlstractor`](https://wenxuan-lu.github.io/tlstractor/reference/tlstractor.md)
for details.

### 3. Setting the number of cores

[`tlstractor()`](https://wenxuan-lu.github.io/tlstractor/reference/tlstractor.md)
supports multi-core computation. By default, the package attempts to
detect the number of available cores and avoid using more cores than
appropriate for the current computing environment. If core detection
does not work as expected, you can manually override the detected number
of available cores:

``` r

# For example, set the number of available cores to 4.
options(
  parallelly.availableCores.custom = function() 4L,
  parallelly.availableCores.methods = "custom"
)
```

On shared clusters, make sure this number does not exceed the number of
cores requested from the scheduler.

[Back to Contents](#contents)

## Installation troubleshooting

Read this section only if installation fails.

### 1. Check R/Bioconductor dependencies

If installation fails while installing or loading `gdsfmt`, install it
explicitly from Bioconductor:

``` r

install.packages("BiocManager")
BiocManager::install("gdsfmt")
```

Then retry installing `tlstractor`.

### 2. Check system build requirements

`tlstractor` contains C++ code and links against `zlib`. Installation
may fail if compilers, `make`, `pkg-config`, or the `zlib` development
files are missing.

#### Quick check

Run `Sys.which(c("make", "g++", "pkg-config"))` in R. `make` and `g++`
should be non-empty. `pkg-config` is recommended because it helps the
installer locate `zlib`, but installation may still succeed without it
if `zlib` is available in standard compiler/linker paths.

To check whether `zlib` is visible through `pkg-config`, run
`system("pkg-config --exists zlib") == 0` in R. This should return
`TRUE`. If `pkg-config` is not installed, skip the `zlib` check and
install the required tools below.

#### Install required tools

- **Windows**: install Rtools matching your R version.
- **macOS**: run `xcode-select --install`, then install `zlib` and
  `pkg-config`, for example with `brew install zlib pkg-config`.
- **Debian/Ubuntu**: run
  `sudo apt-get install -y build-essential zlib1g-dev pkg-config`.
- **Fedora/RHEL/CentOS**: run
  `sudo dnf install -y gcc gcc-c++ make zlib-devel pkgconf-pkg-config`.
- **Alpine**: run `apk add --no-cache build-base zlib-dev pkgconf`.

Then retry installing `tlstractor`.

#### Advanced: manual `zlib` configuration

This is only needed if installation still fails after `zlib` has been
installed.

Set `zlib` paths explicitly, replacing the example paths with the
locations on your system:

``` r

Sys.setenv(
  ZLIB_CPPFLAGS = "-I/path/to/include",
  ZLIB_LIBS = "-L/path/to/lib -lz"
)
```

Then retry installation.

[Back to Contents](#contents)

## Citation

If you use `tlstractor`, please cite the TLS-Tractor manuscript:

> Lu, W., Zhao, R., Chatterjee, N. TLS-Tractor: A transfer learning
> framework for incorporating summary-statistics into local
> ancestry-aware GWAS in admixed populations. *medRxiv* (2026).
> [Link](https://doi.org/10.64898/2026.08.04.26359626)

TLS-Tractor extends the Tractor framework, so please also cite the
original Tractor paper:

> Atkinson, E.G., Maihofer, A.X., Kanai, M. et al. Tractor uses local
> ancestry to enable the inclusion of admixed individuals in GWAS and to
> boost power. *Nature Genetics* **53**, 195–204 (2021).
> [Link](https://www.nature.com/articles/s41588-020-00766-y)

[Back to Contents](#contents)

## Bug reports and questions

For bugs, feature requests, usage questions, or other feedback, please
open a GitHub issue at
<https://github.com/Wenxuan-Lu/tlstractor/issues>.

[Back to Contents](#contents)

## License

`tlstractor` is licensed under the MIT License. See
[LICENSE](https://wenxuan-lu.github.io/tlstractor/LICENSE).

[Back to Contents](#contents)

# Package index

## Extract tracts and ancestry dosages

- [`extract_tracts()`](https://wenxuan-lu.github.io/tlstractor/reference/extract_tracts.md)
  : Extract Local-Ancestry Tracts from Phased VCF and MSP
  (RFMix2/Gnomix)
- [`extract_tracts_flare()`](https://wenxuan-lu.github.io/tlstractor/reference/extract_tracts_flare.md)
  : Extract Local-Ancestry Tracts from a FLARE VCF file
- [`convert_gds_to_txt()`](https://wenxuan-lu.github.io/tlstractor/reference/convert_gds_to_txt.md)
  : Convert Local-Ancestry Tracts Format from GDS to Text
- [`convert_txt_to_gds()`](https://wenxuan-lu.github.io/tlstractor/reference/convert_txt_to_gds.md)
  : Convert Local-Ancestry Tracts Format from Text to GDS

## Munge external GWAS summary statistics

- [`munge_sumstats()`](https://wenxuan-lu.github.io/tlstractor/reference/munge_sumstats.md)
  : Munge GWAS Summary Statistics to Align with GDS File

## Run TLS-Tractor

- [`tlstractor()`](https://wenxuan-lu.github.io/tlstractor/reference/tlstractor.md)
  : Run TLS-Tractor

# Articles

### All vignettes

- [TLS-Tractor
  Tutorial](https://wenxuan-lu.github.io/tlstractor/articles/tlstractor-tutorial.md):
