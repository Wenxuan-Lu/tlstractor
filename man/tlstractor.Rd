% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tlstractor.R
\name{tlstractor}
\alias{tlstractor}
\title{Run TLS-Tractor}
\usage{
tlstractor(
  gds_path,
  sumstats_path,
  method,
  cond_local,
  pheno_path,
  pheno_id_col,
  pheno_col,
  covar_path = NULL,
  covar_id_col = NULL,
  covar_cols = NULL,
  output_prefix,
  scratch_dir = NULL,
  snp_start = 1L,
  snp_count = NULL,
  n_cores = 1L,
  chunk_size = 1024L,
  local_ancestry_mac_threshold = 20L,
  use_fast_version = TRUE
)
}
\arguments{
\item{gds_path}{Character; path to the input GDS file.}

\item{sumstats_path}{Character; path to the munged summary statistics file.}

\item{method}{Character; one of \code{"linear"} or \code{"logistic"}.}

\item{cond_local}{Logical; whether to condition on local-ancestry dosage
terms.}

\item{pheno_path}{Character; path to the phenotype file.}

\item{pheno_id_col}{Character; sample-ID column name in the phenotype file.}

\item{pheno_col}{Character; phenotype column name in the phenotype file.}

\item{covar_path}{Character or \code{NULL}; path to an optional covariate file.
Default is \code{NULL}.}

\item{covar_id_col}{Character or \code{NULL}; sample-ID column name in the
covariate file (required when \code{covar_path} is provided). Default is \code{NULL}.}

\item{covar_cols}{Character vector or \code{NULL}; covariate column names in the
covariate file (required when \code{covar_path} is provided). Default is \code{NULL}.}

\item{output_prefix}{Character; output path prefix.}

\item{scratch_dir}{Character or \code{NULL}; directory used to temporarily store
per-task result files before merge. If \code{NULL}, a run-specific directory is
created under \code{dirname(output_prefix)} with name
\verb{<basename(output_prefix)>_<pid>_<YYYYmmdd_HHMMSS>_tmp}. Default is \code{NULL}.}

\item{snp_start}{Integer; 1-based starting SNP index in the GDS file.
Default is \code{1L}.}

\item{snp_count}{Integer or \code{NULL}; number of SNPs to process. If \code{NULL},
processing starts at \code{snp_start} and continues to the end of the GDS file.
Default is \code{NULL}.}

\item{n_cores}{Integer; number of CPU cores to use. Default is \code{1L}.}

\item{chunk_size}{Integer; number of variants processed per chunk within each
CPU core. Default is 1024; larger values may improve speed but increase
memory usage.}

\item{local_ancestry_mac_threshold}{Integer; ancestry-specific minor-allele
count threshold. SNPs with any ancestry-specific MAC below this threshold
are skipped. Skipped SNPs are returned in the output with \code{NA} values for
inferential columns. Default is \code{20L}.}

\item{use_fast_version}{Logical; whether to use the fast TLS-Tractor mode.
The fast mode assumes that the estimated non-genetic covariate effects from
the null model (phenotype ~ covariates) are close to those from the full
standard GWAS model (phenotype ~ SNP dosage + covariates) for any single
SNP, so that estimated covariate effects from the null model can be reused
across variants to reduce computation. The fast mode can provide a
substantial speedup with minimal loss of accuracy. Default is \code{TRUE}
(recommended for large datasets). Setting \code{FALSE} fources the full
per-variant fitting path, which is generally more robust but substantially
slower when covariates are included. If no covariates are provided, fast
mode is automatically disabled.}
}
\value{
Invisibly returns \code{NULL}. Writes gzipped GWAS results to
\verb{<output_prefix>.txt.gz}. The output includes:
\itemize{
\item Variant metadata: \code{CHROM}, \code{POS}, \code{ID}, \code{REF}, \code{ALT}, \code{main_N}
(sample size in the main/internal study)
\item Frequency and ancestry summaries: \code{AF} (overall allele frequency),
\verb{AF_anc*} (ancestry-specific allele frequency), \verb{LAprop_anc*}
(local-ancestry-specific haplotype proportion)
\item Analysis metadata: \code{has_sumstats} (indicates whether external
summary statistics were available), \code{fallback_used} (indicates whether
QR decomposition fallback was used; when TRUE, results may have reduced
numerical stability and should be interpreted with caution)
\item Association results: \code{joint_pval} (p-value for joint testing all
ancestry-specific SNP dosage effects), \verb{beta_anc*}, \verb{se_anc*},
\verb{pval_anc*} (effect size, standard error, and p-value for each
ancestry-specific SNP dosage effect)
\item When \code{cond_local = TRUE}: \verb{LAeff_anc*}, \verb{LAse_anc*}, \verb{LApval_anc*}
(effect size, standard error, and p-value for each local ancestry term)
}
}
\description{
Performs local ancestry-aware GWAS by integrating individual-level data with
external GWAS summary statistics via transfer learning.
}
\details{
If present, output the file \verb{<output_prefix>.excluded_samples.txt} containing
sample IDs present in the GDS file but excluded from analysis after sample
intersection/filtering.

During execution, temporary per-task result files are written to
\verb{scratch_dir/<run_tag>_task_<id>.txt.gz}, where \code{run_tag} is a run-specific
identifier with format \verb{tlstractor_<pid>_<YYYYmmdd_HHMMSS>}. These temporary
files are removed on successful cleanup. The directory \code{scratch_dir} is
removed only if it was created by the function.

SNPs present in the GDS file but absent from the summary statistics file are
still analyzed using the Tractor model with individual-level data only.

To perform Tractor anlysis on all SNPs using only the main study,
set \code{sumstats_path} to a file with one tab-delimited header line containing:
\code{CHR}, \code{POS}, \code{ID}, \code{REF}, \code{ALT}, \code{BETA}, \code{SE}, and \code{GDS_ID}.
}
