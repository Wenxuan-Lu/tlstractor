% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_tracts_flare.R
\name{extract_tracts_flare}
\alias{extract_tracts_flare}
\title{Extract Local-Ancestry Tracts from a FLARE VCF file}
\usage{
extract_tracts_flare(
  vcf_path,
  num_ancs,
  output_dir = NULL,
  output_formats = "gds",
  chunk_size = 1024L
)
}
\arguments{
\item{vcf_path}{Character; input FLARE VCF path (\code{.vcf} or \code{.vcf.gz}).}

\item{num_ancs}{Integer; number of ancestral populations (must be > 1).}

\item{output_dir}{Character or \code{NULL}; output directory (must exist). If
\code{NULL} (default), uses the directory of \code{vcf_path}.}

\item{output_formats}{Character vector; output format specification (for
example \code{"gds"} or \code{c("gds", "txt.gz")}). Default is \code{"gds"}.
Supported values: \code{gds}, \code{txt}, \code{txt.gz}, \code{vcf}, \code{vcf.gz}.
If both compressed and uncompressed versions are requested for the same
type, compressed output is written: \code{txt.gz} over \code{txt}, and \code{vcf.gz}
over \code{vcf}.}

\item{chunk_size}{Integer; number of variants processed per chunk.
Default is 1024; higher values can improve speed but increase memory usage.}
}
\value{
Invisibly returns \code{NULL}. Output files are written with filename
prefix derived from \code{vcf_path} in \code{output_dir}.
}
\description{
Parses a FLARE VCF and writes ancestry-specific outputs in one or more formats:
\code{gds}, \code{txt}, \code{txt.gz}, \code{vcf}, \code{vcf.gz}.
}
\details{
Output files (prefix = basename of \code{vcf_path} without \code{.vcf} or \code{.vcf.gz}):
\itemize{
\item \code{gds}: one file \verb{<prefix>.gds} containing \code{sample.id}, variant metadata
(\code{snp.chromosome}, \code{snp.position}, \code{snp.id}, \code{snp.ref}, \code{snp.alt}), and
ancestry-specific nodes \code{dosage/anc0..ancK} and \code{hapcount/anc0..ancK}.
\item \code{txt} / \code{txt.gz}: for each ancestry \code{k}, two files
\verb{<prefix>.anc\{k\}.dosage.txt(.gz)} and \verb{<prefix>.anc\{k\}.hapcount.txt(.gz)}.
Columns are \verb{CHROM POS ID REF ALT} followed by one column per sample.
\item \code{vcf} / \code{vcf.gz}: for each ancestry \code{k}, one file \verb{<prefix>.anc\{k\}.vcf(.gz)}
with \code{FORMAT=GT}; haplotypes not assigned to ancestry \code{k} are written as \code{.}.
}

Assumptions for the input FLARE VCF:
\itemize{
\item Biallelic variants.
\item No missing genotype/local-ancestry fields.
\item No duplicate variants.
\item Autosomal chromosomes only (1-22, 01-22, \code{chr1}-\code{chr22}, \code{chr01}-\code{chr22}; case-insensitive).
\item Alleles are uppercase \code{A/C/G/T}.
\item Sample FORMAT is exactly \code{GT:AN1:AN2}.
\item \code{GT} is phased (\code{0|0}, \code{0|1}, \code{1|0}, \code{1|1}) with single-digit allele codes.
}
}
